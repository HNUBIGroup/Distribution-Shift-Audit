# Source Package Placeholder

The current research code is script-oriented and remains at the repository top level plus `scripts/` to avoid changing validated experimental entry points.

Future cleanup can move reusable modules into `src/`, but that should be done only with regression tests against frozen source data.

