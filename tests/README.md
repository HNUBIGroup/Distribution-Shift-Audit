# Tests

Minimal packaging checks are currently provided through:

```bash
python scripts/check_environment.py
bash scripts/run_smoke_test.sh
python scripts/verify_official_assets.py --allow-missing
```

Add unit tests here when refactoring script-oriented research code into reusable modules.
