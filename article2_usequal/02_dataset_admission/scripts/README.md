# Admission scripts boundary

Scripts added here may inspect local metadata, manifests, and official loader source without downloading large assets or training a model. They must write only under `02_dataset_admission/` and record source/version hashes when available.
