# Evidence ledger

Store one evidence record per factual assertion as `dataset_id__source_slug.md`. Each record must contain: dataset ID, field(s) supported, official URL or immutable local path, source type, access date, quoted locator/section, extraction notes, and `VERIFIED`/`UNRESOLVED` status. Candidate source locators in the registry are not evidence until inspected.
