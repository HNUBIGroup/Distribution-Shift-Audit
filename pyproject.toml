[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sparse-rna-reliability-audit"
version = "0.1.0"
description = "Research code and source data packaging for sparse RNA association prediction reliability audits."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "License information will be finalized by the repository owners." }
authors = [
  { name = "HNUBIGroup" }
]

[tool.setuptools]
py-modules = []
